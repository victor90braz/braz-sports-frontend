import styled from "styled-components";

const EventDescriptionStyle = styled.div`
  .card {
    display: flex;
    flex-direction: column;
    justify-content: center;
  }

  ul {
    padding: 0;
    list-style: none;

    li {
      margin-bottom: 10px;
    }
  }
`;

export default EventDescriptionStyle;
