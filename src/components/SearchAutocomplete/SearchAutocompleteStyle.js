import styled from "styled-components";

const SearchAutocompleteStyle = styled.div`
  display: flex;
  flex-direction: column;
  align-items: center;
  input {
    margin: 10px;
  }
  li {
    text-transform: uppercase;
  }
`;

export default SearchAutocompleteStyle;
