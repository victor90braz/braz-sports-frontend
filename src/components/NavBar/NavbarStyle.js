import styled from "styled-components";

const NavbarStyle = styled.div`
  .navbar-brand {
    display: flex;
    align-items: center;
    .brand {
      width: 5rem;
    }
  }
`;

export default NavbarStyle;
