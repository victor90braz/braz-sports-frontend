import styled from "styled-components";

const NavbarStyle = styled.div`
  .navbar {
    display: contents;

    .navbar-brand {
      display: flex;
      align-items: center;
      .brand {
        width: 8rem;
      }
    }
  }
`;

export default NavbarStyle;
