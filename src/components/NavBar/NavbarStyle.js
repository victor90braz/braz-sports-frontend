import styled from "styled-components";

const NavbarStyle = styled.div`
  .navbar-brand {
    display: flex;
    align-items: center;
    font-size: 1.5rem;
    .brand {
      width: 5rem;
    }
  }
`;

export default NavbarStyle;
